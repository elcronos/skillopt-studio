"""API routers for SkillOpt Studio.

Routers are imported defensively by ``main.py`` so that not-yet-built routers do
not crash startup. Only ``health`` is guaranteed to exist at the foundation stage.
"""
